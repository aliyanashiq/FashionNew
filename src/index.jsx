import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // Optional: Ensure you have this file or remove the line if not needed
import App from './App'; // Optional: Ensure App.js exists or adjust according to your structure

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
